/*
let usuario = {
    nombre : 'Tiago',
    apellido : "Cuello",
    edad : '19' ,
    mascota : true,
} ;

let unirNombre = () => console.log(usuario.nombre + ' ' + usuario.apellido) 
    
unirNombre()

console.log(usuario,apellido) ;
*/

// let listaCompras

function noParesDeContarImparesHasta(i) {
    for ( let i = 2 ; i <= 4 ;i++) {
       if(i % 2 === 0) {
       return console.log(i + 1) }
       return console.log(i)}
       }

       noParesDeContarImparesHasta()



